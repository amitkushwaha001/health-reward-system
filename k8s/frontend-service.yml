apiVersion: v1
kind: Service

metadata:
  name: health-frontend-service

spec:
  selector:
    app: health-frontend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30001

  type: NodePort